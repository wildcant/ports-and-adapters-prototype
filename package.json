{
  "name": "ports-and-adapters-prototype",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "setup": "npm install && npm run --workspace=backend db:setup",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check . && node apps/backend/scripts/check-module-boundaries.js",
    "openapi:generate": "npm run --workspace=backend openapi:dump:offline && npm run --workspace=frontend openapi:client",
    "typecheck": "npm run --workspace=backend typecheck && npm run --workspace=frontend typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  }
}
